global:
  scrape_interval: 10s
  evaluation_interval: 30s

scrape_configs:
  - job_name: pv-exporter
    static_configs:
      - targets: ["pv-exporter:9114"]

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
