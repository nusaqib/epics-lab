# ChannelFinder client configuration used by the recceiver `cf` processor.
# Credentials must match demo_auth in channelfinder/application.properties.
[DEFAULT]
BaseURL=http://channelfinder:8080/ChannelFinder
username=admin
password=adminPass
